# IMPORTANT: This file is a template only. Do NOT commit real credentials.
#
# When S3 auth is enabled (Phase 3+), create a Secret with the following format
# and set filer.s3.enableAuth=true and filer.s3.existingConfigSecret=seaweedfs-s3-secret
# in infra/seaweedfs/values.yaml.
#
# The Secret must contain a key named "seaweedfs_s3_config" with an inline JSON config:
#
#   kubectl create secret generic seaweedfs-s3-secret \
#     -n seaweedfs \
#     --from-literal=seaweedfs_s3_config='{"identities":[{"name":"admin","credentials":[{"accessKey":"REPLACE_ME","secretKey":"REPLACE_ME"}],"actions":["Admin","Read","Write","List","Tagging"]}]}'
#
# Do not apply this file directly — apply the kubectl command above manually.
