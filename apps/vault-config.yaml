apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-config
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  project: default
  source:
    repoURL: https://github.com/lustoerk/k8ts.git
    targetRevision: main
    path: infra/vault-config
  destination:
    server: https://kubernetes.default.svc
    namespace: external-secrets
  # No automated sync — must be triggered manually after Vault is initialized
  # and Kubernetes auth is configured. See docs/task-vault.md Step 4.
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
